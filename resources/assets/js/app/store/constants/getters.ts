export const fileTypes = (state) => {
    return state.FILE_TYPES;
};

export const analysisOptions = (state) => {
    return state.ANALYSIS_SETTINGS;
};

export const proseWrapOptions = (state) => {
    return state.PROSE_WRAP;
};

export const arrowParentOptions = (state) => {
    return state.ARROW_PARENTS;
};

export const trailingCommaOptions = (state) => {
    return state.TRAILING_COMMAS;
};

export const patchStatuses = (state) => {
    return state.PATCH_STATUSES;
};

